---
driver_version: 1
generated_at: 2026-06-08T05:20:36Z
generated_by: work-driver-prep
source:
  project: dossier
  phase: dossier-polish-2026-06-07
repo: dossier
repo_url: https://github.com/itsHabib/dossier
branch_prefix: polish-
default_runtime: local

batches:
  - id: 1
    label: ready now
    depends_on: []
    status: pending
    streams:
      - task_id: tsk_01KTJSHNQES9PTD0G69D5F2NVP
        task_slug: ci-cargo-audit
        spec_path: docs/features/dossier-polish-2026-06-07/ci-cargo-audit.md
        branch_name: polish-ci-cargo-audit
        runtime: local
        touches: [.github/workflows/audit.yml, .github/workflows/ci.yml]
        status: pending
      - task_id: tsk_01KTJSJGP58B2BZ6Q2A9TSP9HH
        task_slug: doc-comment-s3store-public-surface
        spec_path: docs/features/dossier-polish-2026-06-07/doc-comment-s3store-public-surface.md
        branch_name: polish-doc-s3store
        runtime: local
        touches: [src/s3store.rs]
        status: pending
      - task_id: tsk_01KTJSJSXCMTF3T5N7BNBQDCFP
        task_slug: mcp-stdio-transport-e2e
        spec_path: docs/features/dossier-polish-2026-06-07/mcp-stdio-transport-e2e.md
        branch_name: polish-mcp-stdio-e2e
        runtime: local
        touches: [tests/mcp_stdio_e2e.rs, Cargo.toml]
        status: pending

conflict_notes:
  - kind: none
    note: "All three streams touch disjoint file sets — CI config (.github/workflows), src/s3store.rs, and a new tests/mcp_stdio_e2e.rs. No source file is shared. No dep signals found in any task body (no depends-on/after/requires verbs referencing another task in this batch). Single parallel-safe batch."

skipped_during_resolution: []
---

# Polish — 2026-06-07 audit driver manifest

Generated by `/work-driver-prep project:dossier:phase:dossier-polish-2026-06-07` on 2026-06-08.
Consumed by `/work-driver docs/features/dossier-polish-2026-06-07/driver.md`.

## Source

Phase `dossier-polish-2026-06-07` ("Polish — 2026-06-07 audit") in project `dossier` — 3 todo tasks at time of prep.

## Topology

One batch. All three tasks touch disjoint files, so they fan out as 3 parallel streams with no ordering constraint. No Batch 2+.

## Batches

### Batch 1 — ready now (3 parallel-safe, all local)

| Task | Touches | Runtime |
|---|---|---|
| `ci-cargo-audit` | `.github/workflows/` (new `audit.yml` or a job in `ci.yml`) — config only | local |
| `doc-comment-s3store-public-surface` | `src/s3store.rs` (doc comments only) | local |
| `mcp-stdio-transport-e2e` | new `tests/mcp_stdio_e2e.rs` (+ maybe `Cargo.toml` dev-dep) | local |

## Conflict scan

```
File overlap: 0 pairs
  ci-cargo-audit          → .github/workflows/*
  doc-comment-s3store...  → src/s3store.rs
  mcp-stdio-transport-e2e → tests/mcp_stdio_e2e.rs (+ Cargo.toml [dev-dependencies], additive)
Dep signals : 0 edges
No deps found between: {ci-cargo-audit, doc-comment-s3store-public-surface, mcp-stdio-transport-e2e}
```

The "no deps found" line is intentional — the scan looked for depends-on/after/requires verbs referencing a sibling task and found none. The s3store task references PRs #48 / #64, but those are merged PRs, not tasks in this batch — no edge.

## Runtime selection

All three are single-repo L1/L2 work with no special-dependency signal (no docker/localhost services, no operator-personal secrets, no browser automation, no multi-repo touch). Default `local` per the runtime heuristic. The MCP E2E test spawns the `dossier` binary as a child process — that's a local build, which local handles cleanly; no cloud signal.

## Note on landing branch

This prep ran with the working tree on `chore/seed-ship-subagents`, which is well behind `main` (main has `s3store.rs` and all four workflows; the chore branch does not). The audit was taken against `main`, and these specs are grounded against `main`. They must be committed onto `main` (or merged via a tiny docs PR) before `/work-driver` fans out, so each worktree inherits the spec docs.
