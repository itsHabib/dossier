---
driver_version: 1
generated_at: 2026-05-31T22:10:59Z
generated_by: work-driver-prep
source:
  project: dossier
  phase: policy-mechanism-separation
repo: dossier
repo_url: https://github.com/itsHabib/dossier
branch_prefix: policy-mech/
default_runtime: local

batches:
  - id: 1
    label: ready now
    depends_on: []
    status: done
    streams:
      - task_id: tsk_01KSZT03F9GB5YNRATRM4BAX70
        task_slug: domain-extract-pure-core
        spec_path: docs/features/policy-mechanism-separation/domain-extract-pure-core.md
        branch_name: policy-mech/domain-extract-pure-core
        runtime: local
        touches: [src/domain.rs, src/store.rs, tests/proptest_state_machine.rs]
        status: done
        pr_number: 67
        merge_commit: fa5278b8be13c773954eb4e0a285c175a65c97e5
        merged_at: 2026-06-01T00:36:00Z
        cycles: 1

  - id: 2
    label: after batch 1
    depends_on: [1]
    status: done
    streams:
      - task_id: tsk_01KSZT0833NJTH8Q1HE8ZCHQVZ
        task_slug: lift-task-state-machine
        spec_path: docs/features/policy-mechanism-separation/lift-task-state-machine.md
        branch_name: policy-mech/lift-task-state-machine
        runtime: local
        touches: [src/server.rs, src/store.rs, tests/proptest_state_machine.rs]
        status: done
        pr_number: 68
        merge_commit: b83e0bac21e8b2341310a83c185e30befbfe2875
        merged_at: 2026-06-01T14:34:00Z
        cycles: 1
        note: "review found a real CAS-atomicity bug (task verbs dropped write_lock); fixed via FsStore-internal cas_lock. Cursor fix-run failed on a DB-lock at commit; recovered manually."

  - id: 3
    label: after batch 2
    depends_on: [2]
    status: done
    streams:
      - task_id: tsk_01KSZT09V65EJJYGMBZC2NJH9B
        task_slug: lift-phase-project-artifact-drop-fs
        spec_path: docs/features/policy-mechanism-separation/lift-phase-project-artifact-drop-fs.md
        branch_name: policy-mech/lift-phase-project-artifact-drop-fs
        runtime: local
        touches: [src/server.rs, src/store.rs]
        status: done
        pr_number: 69
        merge_commit: dad77e467951311778ea4e3251519996a7574d82
        merged_at: 2026-06-01T15:25:00Z
        cycles: 1
        note: "shift_phases lifted to the Store trait (option A) on FsStore + S3Store; fs field dropped. 3 review nits fixed inline; slug-validation + S3-idempotency/perf + CLI-double-read deferred to follow-up tasks."

conflict_notes:
  - kind: file_overlap
    file: src/store.rs
    tasks: [domain-extract-pure-core, lift-task-state-machine, lift-phase-project-artifact-drop-fs]
    note: "all three units edit store.rs in sequence — unit 1 makes methods delegate, unit 2 deletes the task methods, unit 3 deletes the rest. Sequential by construction; the dep chain already serializes them."
  - kind: file_overlap
    file: src/server.rs
    tasks: [lift-task-state-machine, lift-phase-project-artifact-drop-fs]
    note: "unit 2 lifts task verbs into MeshService; unit 3 lifts the rest + drops the fs field. Same struct, sequential via the dep edge."
  - kind: file_overlap
    file: tests/proptest_state_machine.rs
    tasks: [domain-extract-pure-core, lift-task-state-machine]
    note: "unit 1 retargets the proptest onto the pure fns; unit 2 reseeds it via the service. Sequential via the dep edge."
  - kind: dep_signal
    from: lift-task-state-machine
    to: domain-extract-pure-core
    reason: "lifted verbs call the pure transition fns extracted in unit 1 (explicit 'Depends on' in the task body)."
  - kind: dep_signal
    from: lift-phase-project-artifact-drop-fs
    to: lift-task-state-machine
    reason: "drops the fs field the task verbs rely on until they're lifted (explicit 'Depends on' in the task body)."
---

# Policy/mechanism separation driver manifest

Generated by `/work-driver-prep project:dossier:phase:policy-mechanism-separation` on 2026-05-31.
Consumed by `/work-driver docs/features/policy-mechanism-separation/driver.md`.

**Status: COMPLETE (2026-06-01).** All 3 batches merged in dep order — #67 (`fa5278b8`) → #68 (`b83e0bac`) → #69 (`dad77e46`). Phase `policy-mechanism-separation` is done; `MeshService` runs on `Arc<dyn Store>` alone, all 9 write verbs over the trait. Follow-ups tracked as dossier tasks: `wire-s3-backend-bin-config` (the payoff), `service-slug-validation`, `s3-shift-phases-idempotency-perf`, `cli-artifact-double-read`.

## Source

Phase `policy-mechanism-separation` in project `dossier` — 3 todo tasks at time of prep. Phase envelope: ~730–900 LOC across 3 PR-sized units (per the phase body). All three units are single-repo Rust impl on `main` → **runtime: local**.

## Topology

**Strict linear chain — zero parallelism available.** The three units serialize two ways at once: (a) all three edit `src/store.rs`, and (b) each explicitly `Depends on` the prior in its task body. Both the file-overlap graph and the dep graph collapse to one line:

```
domain-extract-pure-core  →  lift-task-state-machine  →  lift-phase-project-artifact-drop-fs
   (unit 1, batch 1)            (unit 2, batch 2)             (unit 3, batch 3)
```

Three batches, one stream each. `/work-driver` runs batch 1 to merge, fast-forwards, then batch 2, then batch 3. This is a sequenced pipeline, not a fan-out — the manifest exists for state tracking + resume, not for concurrency.

## Batches

### Batch 1 — ready now (1 stream)

| Task | Spec | Touches | Runtime |
|---|---|---|---|
| `domain-extract-pure-core` | [domain-extract-pure-core.md](domain-extract-pure-core.md) | `src/domain.rs`, `src/store.rs`, `tests/proptest_state_machine.rs` | local |

Extract the pure core (transitions, order, validation, id, arg-structs) into `domain.rs`; `FsStore` methods delegate. No behavior change.

### Batch 2 — after Batch 1 merges (1 stream)

| Task | Spec | Touches | Runtime |
|---|---|---|---|
| `lift-task-state-machine` | [lift-task-state-machine.md](lift-task-state-machine.md) | `src/server.rs`, `src/store.rs`, `tests/proptest_state_machine.rs` | local |

Lift the 4 task verbs into `MeshService` as get→transition→put(CAS) loops; delete the inherent `FsStore` task methods; add the concurrent-claim race test.

### Batch 3 — after Batch 2 merges (1 stream)

| Task | Spec | Touches | Runtime |
|---|---|---|---|
| `lift-phase-project-artifact-drop-fs` | [lift-phase-project-artifact-drop-fs.md](lift-phase-project-artifact-drop-fs.md) | `src/server.rs`, `src/store.rs` | local |

Lift the remaining 5 verbs (phase/project/artifact), strip `FsStore` to the `Store` trait + `open`/`root`, and **drop the `fs: Arc<FsStore>` field** — service now runs on `Arc<dyn Store>` alone.

## Conflict topology

`src/store.rs` is touched by all three units; `src/server.rs` by units 2–3; `tests/proptest_state_machine.rs` by units 1–2. Every overlap is already serialized by the dep chain, so the file conflicts impose no ordering beyond what the deps already require. There is no batch where two streams could safely run in parallel.

## Dep signals

Both edges are explicit `Depends on` lines in the task bodies (not inferred):

- `lift-task-state-machine` → `domain-extract-pure-core` (lifted verbs call the extracted pure fns).
- `lift-phase-project-artifact-drop-fs` → `lift-task-state-machine` (drops the `fs` field the task verbs need until lifted).

## Cross-cutting decisions (from the phase body — confirm at review of each PR)

- **`write_lock` fate** → KEEP the process-local `Mutex` as FsStore's intra-process guard; the get→put CAS is the cross-writer authority. Don't change two things at once.
- **FsStore shims vs full strip** → FULL STRIP to the `Store` trait + `open`/`root`; rewrite test-seed helpers to seed via the service.
- **pure-fn home** → `src/domain.rs` (no I/O, protocol-shaped).
- **`task.create` uniqueness (unit 2)** *(surfaced in #66 review)* → `create` is project-scoped, not self-CAS: gate slug-uniqueness on a `put_project(expected=v)` CAS (recommended, mirrors `add_phase`), not `put_task` on the new id. Confirm the mechanism at impl; see [lift-task-state-machine.md](lift-task-state-machine.md).
- **`add_phase` shift mechanism (unit 3)** *(surfaced in #66 review)* → `Arc<dyn Store>` has no atomic rename, so the displaced-phase shift needs a declared primitive: **option A** (a `shift_phases` mechanism on the `Store` trait — recommended, keeps policy in the service) vs **option B** (composite `add_phase` on the trait). Confirm at impl; see [lift-phase-project-artifact-drop-fs.md](lift-phase-project-artifact-drop-fs.md).
- **retry budget** *(surfaced in #66 review)* → all lifted CAS loops use cloud spec §8 verbatim (`base=25ms`, `cap=2s`, max 5, typed `conflict` on exhaustion); supersedes the older `PHASE_ADD_MAX_RETRIES=8`.

## Validation gate (every unit)

Behavior parity: existing `MeshService` verb tests + `tests/proptest_state_machine.rs` green at every step; the state-machine proptest retargeted onto the extracted pure fns; the new service-layer concurrent-claim CAS test (one winner, loser re-reads to terminal); `make check` (fmt + clippy-strict `-D warnings` + test) green per unit.

## Next-unblock (not yet a task)

Once unit 3 lands: wire S3 into bin + config (`DOSSIER_BACKEND=s3`, construct `S3Store`) so the server boots on S3 — the payoff. Materialize as a task when unit 3 merges.

Umbrella design: [docs/features/cloud-backend/spec.md](../cloud-backend/spec.md) (§3 architecture, §6 Store trait, §7.1 write flow).
