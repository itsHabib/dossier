---
driver_version: 1
generated_at: 2026-07-24T05:04:49Z
generated_by: work-driver-prep
source:
  project: dossier
  phase: state-substrate-schema
repo: dossier
repo_url: https://github.com/itsHabib/dossier
branch_prefix: state-substrate/
default_runtime: local

batches:
  - id: 1
    label: ready now — base schema
    depends_on: []
    status: pending
    streams:
      - task_id: tsk_01KY86E5R4HW7WCV1DPREQK0WM
        task_slug: artifact-meta-field
        spec_path: docs/features/state-substrate-schema/artifact-meta-field.md
        branch_name: state-substrate/artifact-meta-field
        runtime: local
        model: sonnet
        effort: extra
        touches: [src/domain.rs, src/store.rs, src/s3store.rs, src/server/artifact.rs, PROTOCOL.md, LAYOUT.md]
        status: pending

  - id: 2
    label: after batch 1 — ref filter + conventions
    depends_on: [1]
    status: pending
    streams:
      - task_id: tsk_01KY86EG5KR3Q8SQMK28CYC0C5
        task_slug: artifact-list-ref-filter
        spec_path: docs/features/state-substrate-schema/artifact-list-ref-filter.md
        branch_name: state-substrate/artifact-list-ref-filter
        runtime: local
        model: sonnet
        effort: extra
        touches: [src/store.rs, src/s3store.rs, src/server/artifact.rs, PROTOCOL.md]
        status: pending
      - task_id: tsk_01KY86EVKAK6ZC682MZ3NERY5M
        task_slug: verdict-receipt-conventions
        spec_path: docs/features/state-substrate-schema/verdict-receipt-conventions.md
        branch_name: state-substrate/verdict-receipt-conventions
        runtime: local
        model: sonnet
        effort: extra
        touches: [PROTOCOL.md, LAYOUT.md, CLAUDE.md]
        status: pending

conflict_notes:
  - kind: dep_signal
    from: artifact-list-ref-filter
    to: artifact-meta-field
    reason: "ref filter must return meta in output — needs the meta field shipped first"
  - kind: dep_signal
    from: verdict-receipt-conventions
    to: artifact-meta-field
    reason: "docs transcribe the shipped meta shape + example rows that must parse against it"
  - kind: file_overlap
    file: PROTOCOL.md
    tasks: [artifact-list-ref-filter (verb-table row for artifact.list ref param), verdict-receipt-conventions (well-known-kinds + convention tables section)]
    note: "textually disjoint sub-regions of PROTOCOL.md; batch-2 streams can parallel-run with low rebase risk — second-to-merge does a trivial rebase on origin/main"
---

# State substrate — schema phase driver manifest

Generated by `/work-driver-prep phase state-substrate-schema` on 2026-07-24.
Consumed by `/work-driver docs/features/state-substrate-schema/driver.md`.

Implements **Phase A** of the [state-substrate TDD](../state-substrate/spec.md) — the schema + verbs
that make `verdict`/`receipt` first-class memory. A GO/NO-GO dogfood gate (Phase B) follows once these land.

## Prerequisite (local runtime)

The three spec docs under `docs/features/state-substrate-schema/` must land on `origin/main` before
the driver fans out — the per-stream worktrees fork from `origin/main`. Open one small docs PR
(`docs(state-substrate-schema): spec docs for 3 tasks`) and merge it; **never** push `main` directly.

## Batches

### Batch 1 — ready now (base schema)
- `artifact-meta-field` → `docs/features/state-substrate-schema/artifact-meta-field.md`
  - touches: `src/domain.rs`, `src/store.rs`, `src/s3store.rs`, `src/server/artifact.rs`, `PROTOCOL.md`, `LAYOUT.md`
  - `meta` map end-to-end + caps + meta-immutability dedup (TDD §5/§6/§7.4). `sonnet` / `extra`.

Everything else depends on this — Batch 1 is a single stream by design.

### Batch 2 — after batch 1 (ref filter + conventions), 2 parallel streams
- `artifact-list-ref-filter` → `docs/features/state-substrate-schema/artifact-list-ref-filter.md`
  - touches: `src/store.rs`, `src/s3store.rs`, `src/server/artifact.rs`, `PROTOCOL.md`. `sonnet` / `extra`.
- `verdict-receipt-conventions` → `docs/features/state-substrate-schema/verdict-receipt-conventions.md`
  - touches: `PROTOCOL.md`, `LAYOUT.md`, `CLAUDE.md` (docs-only, 0× weight). `sonnet` / `extra`.

Both depend only on Batch 1. They overlap on `PROTOCOL.md` in disjoint sub-regions (verb-table row
vs. well-known-kinds section) — parallel-safe with low rebase risk; the second to merge rebases on
`origin/main`.
