---
driver_version: 1
generated_at: 2026-05-29T23:56:01Z
generated_by: work-driver-prep
source:
  project: dossier
  phase: s3-cloud
repo: dossier
repo_url: https://github.com/itsHabib/dossier
branch_prefix: cloud-
default_runtime: local

batches:
  - id: 1
    label: store seam — foundation (must land first)
    depends_on: []
    status: pending
    streams:
      - task_id: tsk_01KSV24D8HS85NBZJ587W6TA9R
        task_slug: extract-store-trait
        spec_path: docs/features/cloud-backend/phase-0/extract-store-trait.md
        branch_name: cloud-store-trait
        runtime: local
        touches: [src/store.rs, src/server.rs, src/lib.rs]
        status: pending

  - id: 2
    label: on the seam — parallel-safe (after batch 1)
    depends_on: [1]
    status: pending
    streams:
      - task_id: tsk_01KSV24EGWKFYR6EE0S1S00S9M
        task_slug: search-to-service-layer
        spec_path: docs/features/cloud-backend/phase-0/search-to-service-layer.md
        branch_name: cloud-search-svc
        runtime: local
        touches: [src/store.rs, src/server.rs]
        status: pending
      - task_id: tsk_01KSV24HKSZRA3YCQ76YWKGV62
        task_slug: phase-ordering-concurrency
        spec_path: docs/features/cloud-backend/phase-0/phase-ordering-concurrency.md
        branch_name: cloud-phase-order
        runtime: local
        touches: [src/server.rs, src/store.rs]
        status: pending

conflict_notes:
  - kind: dep_signal
    from: search-to-service-layer
    to: extract-store-trait
    reason: "removes `search` from the Store trait that extract-store-trait creates (D9); task-1 out-of-scope explicitly cites 'the search move (separate task)'"
  - kind: dep_signal
    from: phase-ordering-concurrency
    to: extract-store-trait
    reason: "task body: 'Implement under the Store CAS model from extract-store-trait'"
  - kind: file_overlap
    file: src/store.rs
    tasks: [extract-store-trait (wide — async trait + CAS over whole public surface), search-to-service-layer (search region ~L183-310 + tests ~L4796-4930), phase-ordering-concurrency (phase-order write path ~L730-744)]
    note: "extract-store-trait is UPSTREAM of both others (dep edges → serialized into batch 1, so no live three-way conflict). Between search-to-service-layer and phase-ordering-concurrency the edited regions are textually disjoint (search vs phase-order write) — parallel-safe in batch 2 with low rebase risk. Override to serial if you prefer max caution."
  - kind: file_overlap
    file: src/server.rs
    tasks: [extract-store-trait (store field L41 + .await ripple through handlers), search-to-service-layer (search handler ~L808), phase-ordering-concurrency (phase_add handler ~L560)]
    note: "same shape as src/store.rs — task-1 upstream of both; the search handler and phase_add handler are ~250 lines apart and textually disjoint → batch-2 parallel-safe."

skipped_during_resolution:
  - reason: "TDD spec.md (cloud-backend/spec.md, §6/§8/D9) lives on unmerged PR #59 (branch docs/cloud-backend-rfc), not on main — so worktrees branching off main won't have it"
    workaround: "each Phase-0 spec inlines the design it depends on (Store trait signature, §8 ordering options) so it is self-contained; merging #59 first co-locates spec.md but is not a hard blocker"
---

# dossier cloud-backend — Phase 0 (Store abstraction) driver manifest

Generated by `/work-driver-prep project:dossier:phase:s3-cloud` on 2026-05-29.
Consumed by `/work-driver docs/features/cloud-backend/phase-0/driver.md`.

These three tasks are **Phase 0** of the cloud-backend rollout (TDD §9, "Store abstraction" — ships now, no-regret, ≤200 wLOC target). They form a dependency chain, not a wide fan-out: one foundation task, then two parallel-safe tasks on top of it.

> Phase-slug note: dossier phase is `s3-cloud`; spec docs are co-located with their TDD under `docs/features/cloud-backend/phase-0/` rather than a separate `s3-cloud/` dir, so the Phase-0 impl specs sit next to the design they implement.

## Batches

### Batch 1 — store seam — foundation (1 stream) — runtime: local

- `tsk_01KSV24D8HS85NBZJ587W6TA9R` → [extract-store-trait.md](extract-store-trait.md) — `src/store.rs`, `src/server.rs`, `src/lib.rs`
  - Extract async `Store` trait + `Versioned<T>`/`Version` + SHA-256 CAS; `FsStore: Store`; server holds `Arc<dyn Store>`. Widest task (the `async` ripple). **Must merge before batch 2.**

### Batch 2 — on the seam — parallel-safe (2 streams, after batch 1) — runtime: local

- `tsk_01KSV24EGWKFYR6EE0S1S00S9M` → [search-to-service-layer.md](search-to-service-layer.md) — `src/store.rs` (search region), `src/server.rs` (search handler)
  - Move `search` out of `Store` into `MeshService` (D9). Mostly a relocation.
- `tsk_01KSV24HKSZRA3YCQ76YWKGV62` → [phase-ordering-concurrency.md](phase-ordering-concurrency.md) — `src/server.rs` (`phase_add`), `src/store.rs` (phase-order write)
  - Resolve the concurrent-`phase.add` lost update via the batch-1 CAS model (or immutable-by-insertion).

Both batch-2 streams touch `src/store.rs` + `src/server.rs` but in **disjoint regions** (search vs phase-order), so they run in parallel safely once batch 1 is on `main`. The second to merge rebases on `origin/main` if any unexpected textual overlap surfaces.

## Conflict scan

```
File overlap : 2 files shared, but only across the dep edge (task 1 ↔ {2,3}); tasks 2 ↔ 3 are
               disjoint sub-regions of src/store.rs + src/server.rs → batch-2 parallel-safe.
Dep signals  : 2 edges — search-to-service-layer → extract-store-trait;
                         phase-ordering-concurrency → extract-store-trait.
No deps found between: {search-to-service-layer ↔ phase-ordering-concurrency}.
```

## Run order

Batch 1 must complete (PR merged to `main`) before batch 2 fans out, because both batch-2 tasks build on the `Store` trait + CAS model batch 1 introduces.
