---
driver_version: 1
generated_at: 2026-05-25T00:50:00Z
generated_by: work-driver-prep
source:
  project: dossier
  phase: v0-polish
repo: dossier
repo_url: https://github.com/itsHabib/dossier
branch_prefix: tower/v0-polish-
default_runtime: cloud

batches:
  - id: 1
    label: ready now (all 7 parallel-safe — no file overlap across the polish surface)
    depends_on: []
    status: pending
    streams:
      - task_id: tsk_01KSE9A1N8P81HJF4W13KBTPY1
        task_slug: mutants-workflow-dispatch-job
        spec_path: docs/features/v0-polish/mutants-workflow-dispatch-job.md
        branch_name: tower/v0-polish-mutants
        runtime: cloud
        touches: [.github/workflows/mutants.yml]
        status: pending
      - task_id: tsk_01KSE9ASHQS3WQG1AN0A93AMH7
        task_slug: windows-matrix-test-job
        spec_path: docs/features/v0-polish/windows-matrix-test-job.md
        branch_name: tower/v0-polish-windows
        runtime: cloud
        touches: [.github/workflows/ci.yml]
        status: pending
      - task_id: tsk_01KSE9BMBCGDPCEK738TF5QCCK
        task_slug: coverage-workflow-dispatch-job
        spec_path: docs/features/v0-polish/coverage-workflow-dispatch-job.md
        branch_name: tower/v0-polish-coverage
        runtime: cloud
        touches: [.github/workflows/coverage.yml]
        status: pending
      - task_id: tsk_01KSE9D4VT116994WKF6JPFS90
        task_slug: prune-shipped-followups
        spec_path: docs/features/v0-polish/prune-shipped-followups.md
        branch_name: tower/v0-polish-followups
        runtime: cloud
        touches: [docs/follow-ups.md]
        status: pending
      - task_id: tsk_01KSE9EDSQZYMC625NVSMEZDGT
        task_slug: reconcile-search-and-protocol-drift
        spec_path: docs/features/v0-polish/reconcile-search-and-protocol-drift.md
        branch_name: tower/v0-polish-search-protocol
        runtime: cloud
        touches: [docs/vision.md, docs/PROTOCOL.md]
        status: pending
      - task_id: tsk_01KSE9FEYHJY334TRN3DN2M6E5
        task_slug: readme-polish
        spec_path: docs/features/v0-polish/readme-polish.md
        branch_name: tower/v0-polish-readme
        runtime: cloud
        touches: [README.md]
        status: pending
      - task_id: tsk_01KSE9GD1VPY08WJHRCXV61X9Y
        task_slug: doc-comment-audit-public-surface
        spec_path: docs/features/v0-polish/doc-comment-audit-public-surface.md
        branch_name: tower/v0-polish-doc-audit
        runtime: cloud
        touches: [src/store.rs, src/server.rs, src/domain.rs]
        status: pending

conflict_notes:
  - kind: no_file_overlap
    note: "all 7 streams touch disjoint file sets — the polish surface is naturally distributed (3 distinct workflow files, 4 distinct doc files, src/*.rs isolated to the doc-comment audit). Parallel-safe with zero rebase risk."
  - kind: dep_signal_soft
    from: readme-polish
    to: reconcile-search-and-protocol-drift
    reason: "README's per-primitive doc pointers link to PROTOCOL.md sections. The new-verb additions in reconcile-search-and-protocol-drift are appended (no existing sections renamed), so README's links to project/phase/task/artifact sections stay valid either order. Soft dep only — both can land in parallel."

skipped_during_resolution: []

runtime_notes:
  - default: cloud
    rationale: "Operator directive 2026-05-24 — dogfood cloud cursor across the remainder of the session. All 7 tasks fit cloud cleanly (single-repo, no localhost/docker deps, no operator-personal secrets, no multi-repo refactors). 7 parallel streams exercises cloud's parallel-at-scale capability — exactly what cloud is for."
  - per_stream_overrides: none
---

# v0-polish driver manifest

Generated by `/work-driver-prep project:dossier:phase:v0-polish` on 2026-05-25.
Consumed by `/work-driver docs/features/v0-polish/driver.md`.

## Source

7 todo tasks in dossier's `v0-polish` phase (`phs_01KSE997QX8153N72D0HMZ1WJN`), captured during the 2026-05-24 audit session. Mix of CI workflow additions, doc hygiene, README expansion, and a doc-comment audit pass across the public source surface.

## Topology

**ONE BIG PARALLEL BATCH of 7 streams.** No file overlap anywhere. The polish round is naturally distributed across distinct surfaces:

- 3 CI workflow files (`mutants.yml`, `ci.yml`, `coverage.yml`) — all disjoint
- 4 doc files (`follow-ups.md`, `vision.md` + `PROTOCOL.md`, `README.md`) — all disjoint
- 3 source files for the doc-comment audit (`store.rs`, `server.rs`, `domain.rs`) — isolated from everything else

Estimated wall-clock: ~15–25 min cloud (parallelization-limited by cursor cloud's concurrency cap rather than per-task latency).

## Batch 1 — ready now (7 parallel-safe streams, all runtime: cloud)

| Stream | Spec | Touches | Runtime |
|---|---|---|---|
| `mutants-workflow-dispatch-job` | `mutants-workflow-dispatch-job.md` | `.github/workflows/mutants.yml` (new) | cloud |
| `windows-matrix-test-job` | `windows-matrix-test-job.md` | `.github/workflows/ci.yml` (edit) | cloud |
| `coverage-workflow-dispatch-job` | `coverage-workflow-dispatch-job.md` | `.github/workflows/coverage.yml` (new) | cloud |
| `prune-shipped-followups` | `prune-shipped-followups.md` | `docs/follow-ups.md` (delete entries) | cloud |
| `reconcile-search-and-protocol-drift` | `reconcile-search-and-protocol-drift.md` | `docs/vision.md`, `docs/PROTOCOL.md` (edits) | cloud |
| `readme-polish` | `readme-polish.md` | `README.md` (additions) | cloud |
| `doc-comment-audit-public-surface` | `doc-comment-audit-public-surface.md` | `src/store.rs`, `src/server.rs`, `src/domain.rs` (doc-comment additions) | cloud |

## Conflict topology

Zero file overlap. The only dep signal is a soft one: `readme-polish` adds inline links from `README.md` to `PROTOCOL.md` sections; `reconcile-search-and-protocol-drift` adds new sections to `PROTOCOL.md`. Both can land in parallel because:

- The README links target existing primitive sections (project / phase / task / artifact), not the new ones being added.
- The new sections are appended; existing section anchors stay stable.

Soft dep recorded in `conflict_notes` for transparency, but no batch split needed.

## Runtime selection

**Default: cloud.** Per operator directive 2026-05-24 — dogfood cursor cloud across the remainder of the session. Each task individually fits cloud cleanly:

- Single repo (no multi-repo refactor signal).
- No localhost / docker-compose / setup-local-db deps.
- No operator-personal env vars or tokens not in the cloud VM.
- 7 parallel streams exercises cloud's parallel-at-scale capability — exactly the use case.

If any stream proves cloud doesn't handle the work shape well, operator can re-fire that single stream as `runtime: local` after editing the manifest.

## Invocation

```sh
# Single command — driver walks the batch in parallel:
/work-driver docs/features/v0-polish/driver.md
```

The driver autodetects manifest form via the `driver_version: 1` frontmatter. Progress (PR numbers, merge SHAs, merged-at timestamps) gets written back to this file as each stream lands.
