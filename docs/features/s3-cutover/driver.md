---
driver_version: 1
generated_at: 2026-06-01T15:40:00Z
generated_by: work-driver-prep
source:
  project: dossier
  phase: null   # ad-hoc: 4 project-wide follow-up tasks (no dossier phase); specs grouped under docs/features/s3-cutover/
repo: dossier
repo_url: https://github.com/itsHabib/dossier
branch_prefix: s3-cutover/
default_runtime: local

batches:
  - id: 1
    label: S3 cutover — ready now
    depends_on: []
    status: pending
    streams:
      - task_id: tsk_01KT1WPR0ND4RYCB5MTC5VYANZ
        task_slug: wire-s3-backend-bin-config
        spec_path: docs/features/s3-cutover/wire-s3-backend-bin-config.md
        branch_name: s3-cutover/wire-s3-backend-bin-config
        runtime: local
        touches: [src/server.rs, src/bin/dossier.rs]
        status: pending
      - task_id: tsk_01KT1WQ1NWPVBH5PMEDNJ2N596
        task_slug: s3-shift-phases-idempotency-perf
        spec_path: docs/features/s3-cutover/s3-shift-phases-idempotency-perf.md
        branch_name: s3-cutover/s3-shift-phases-idempotency-perf
        runtime: local
        touches: [src/s3store.rs]
        status: pending

  - id: 2
    label: review hygiene — after batch 1
    depends_on: [1]
    status: pending
    streams:
      - task_id: tsk_01KT1WPV5S2619WMFKEMHB5SDZ
        task_slug: service-slug-validation
        spec_path: docs/features/s3-cutover/service-slug-validation.md
        branch_name: s3-cutover/service-slug-validation
        runtime: local
        touches: [src/server.rs]
        status: pending
      - task_id: tsk_01KT1WQ3M3V9TMCN101YF7RQ8G
        task_slug: cli-artifact-double-read
        spec_path: docs/features/s3-cutover/cli-artifact-double-read.md
        branch_name: s3-cutover/cli-artifact-double-read
        runtime: local
        touches: [src/bin/dossier.rs]
        status: pending

conflict_notes:
  - kind: file_overlap
    file: src/server.rs
    tasks: [wire-s3-backend-bin-config, service-slug-validation]
    note: "sub-region disjoint — wire-s3 edits the MeshService constructor (top of file); service-slug edits the verb handlers (mid-file). Serialized conservatively into separate batches; low rebase risk, safe to collapse into one 4-stream batch if preferred."
  - kind: file_overlap
    file: src/bin/dossier.rs
    tasks: [wire-s3-backend-bin-config, cli-artifact-double-read]
    note: "different functions — wire-s3 edits run_serve; cli-double-read edits run_artifact_link. Disjoint; serialized conservatively, low rebase risk."
  - kind: independent
    file: src/s3store.rs
    tasks: [s3-shift-phases-idempotency-perf]
    note: "s3store.rs only — no overlap with any other stream."

skipped_during_resolution: []
---

# S3 cutover driver manifest

Generated by `/work-driver-prep tsk_01KT1WPR… tsk_01KT1WPV… tsk_01KT1WQ1… tsk_01KT1WQ3…` on 2026-06-01.
Consumed by `/work-driver docs/features/s3-cutover/driver.md`.

## Source

4 project-wide `todo` follow-up tasks from the `policy-mechanism-separation` phase — its named next-unblock (`wire-s3`) + three PR-#69 review follow-ups. No dossier phase (the tasks are project-wide); specs grouped here.

## Topology

Two batches. **Batch 1 = the S3 cutover** (`wire-s3` + the `s3-shift` fix — both exercise the S3 path, both verified against MinIO). **Batch 2 = review hygiene** (`service-slug` + `cli-double-read`), serialized after batch 1 only because each shares a **sub-region-disjoint** file with `wire-s3` (server.rs constructor vs handlers; bin `run_serve` vs `run_artifact_link`). Rebase risk is low — collapsible to a single 4-stream batch if you'd rather.

All four are single-repo Rust → **runtime: local**. `wire-s3` and `s3-shift` exercise the S3 path → need local MinIO for full verification (the FsStore + unit legs still gate without it).

## Batches

### Batch 1 — S3 cutover (2 parallel)

| Task | Spec | Touches |
|---|---|---|
| `wire-s3-backend-bin-config` | [wire-s3-backend-bin-config.md](wire-s3-backend-bin-config.md) | `src/server.rs` (constructor), `src/bin/dossier.rs` (`run_serve`) |
| `s3-shift-phases-idempotency-perf` | [s3-shift-phases-idempotency-perf.md](s3-shift-phases-idempotency-perf.md) | `src/s3store.rs` |

### Batch 2 — review hygiene (2 parallel, after Batch 1)

| Task | Spec | Touches |
|---|---|---|
| `service-slug-validation` | [service-slug-validation.md](service-slug-validation.md) | `src/server.rs` (verb handlers) |
| `cli-artifact-double-read` | [cli-artifact-double-read.md](cli-artifact-double-read.md) | `src/bin/dossier.rs` (`run_artifact_link`) |

## Conflict topology

`wire-s3` is the only stream touching shared files, and only in sub-regions disjoint from its batch-2 neighbors. `s3-shift` is fully independent. If you accept the low rebase risk, all four can run as a single parallel batch.
